# JavaScript Advanced Notes

## Promises
- A Promise represents the eventual result of an async operation
- States: pending, fulfilled, rejected
- Use .then() for success and .catch() for errors

## Async/Await
- sync functions always return a Promise
- wait pauses execution until the Promise resolves
- Makes async code look and behave like synchronous code
